export const Api = function(opts) {
  opts = opts || {};

  const apiKey = opts.apiKey;
  const URL = '';

  const url = () => {
    let url = URL;
    return `${url}`;
  };

  return url();
};

export default Api
